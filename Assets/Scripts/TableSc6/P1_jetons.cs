using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class P1_jeton : MonoBehaviour
{
    public int[] jetonTab ;

    // Start is called before the first frame update
    void Start()
    {
        for (int i = 0; i <jetonTab.Length; i++)
        {
            if (jetonTab[i] != 0)
            {

            }
        }
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
